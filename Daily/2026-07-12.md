# 2026-07-12 — Bell Commander: BNS rebrand + deploy

## UI rebranded to Beyond Network Solutions
Rebranded the "Schoolyard" cream/green/brass theme to the **BNS identity** — pulled the real palette off beyondnetworksolutions.com.au.

- Electric blue `#2870FF` = accent + primary CTA; deep navy `#13294B` chrome; rose `#E11D48` alert (nods to BNS magenta); cool neutrals.
- Blue-gradient header with the **official Beyond wordmark** (inlined SVG, works offline on the Pi) + "Bell Commander" lockup.
- Softer modern card shadows, tighter borders, system-sans type. Tokens remapped in `:root` so the recolour propagated across every screen.
- Fixed brass-theme leftovers exposed by the recolour (brown button text, amber focus/glow rings, warm borders → all cooled to blue).

## Deployed to the office Pi
- Built UI → synced into the appliance's vendored `static/`; pushed to `pi@172.16.200.217` (backed up live `static/` → `static.bak-predeploy` for rollback).
- Verified served JS carries the BNS markers; AHM stayed connected. Rebooted (after hours) so the touch panel reloaded the new look. Appliance healthy, `online:true`.
- Backed up to bns-tooling (`beta`, commit `1c69a22`).

## GitHub auth (Obsidian sync)
- New fine-grained PAT stored in Windows Credential Manager (host github.com, user jamieleepeters123-ctrl); replaced the stale credential. CLI `ls-remote` + `push --dry-run` both pass without a prompt.
- obsidian-git on desktop uses **system git** → same credential path, so no token needed in plugin settings.

## Next — calendar functionality
Reviewing AcroVista BellCommander's Web Manager (`Design/bcwebv2.pdf`) for calendar ideas: week-cycle rotations (A/B weeks), date-range exceptions, per-zone exceptions, in-calendar editing. See [[Beyond Bell Commander]].
