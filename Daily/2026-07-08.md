### Beyond Bell Commander — built & deployed today

Went from demo → **working appliance on the office Pi** (`bellcommander.service` on :80, driving the real office AHM `172.16.200.127`). Full detail in [[Beyond Bell Commander]] → *Prototype build — deployed 8 Jul 2026*.

**Built the engine + UI + commissioning**
- Python engine: scheduler (compile + 1-second tick loop), AHMDriver (Ext-Control :51325), FakeAHM, SQLite audit log, FastAPI. ~78 tests.
- Schoolyard **React UI served from the appliance** — Today / Calendar / Zones / Log / Setup / System.
- Deployed to the office Pi, survives reboot. Touch panel kiosk repointed to the appliance. Old `~/kiosk` build removed.

**Yesterday's settings ideas → done**
- Display sleep + brightness ✅ (DSI panel, `xprintidle` watcher)
- Network Static / DHCP / MAC ✅ (nmcli, with a 90 s auto-revert safety)
- Save / Import / Export ✅ (validated config save + hot-reload)
- System Health / Logs ✅ (audit log + Log tab)
- Custom NTP server ✅ (not on yesterday's list but added)

**Commissioning auto-fill ("point at the AHM + the DoE")**
- Sounds ← AHM library · Zones ← AHM names (+ **rename writes back to the AHM**) · Timetable ← school `schools.nsw.gov.au` page · Term dates + public-holiday names ← NSW DoE.

**Fixes** — special-day gating now overrides holidays/weekends · show template *name* not its id · header badge "Online" · zone mute switch red.

**Tomorrow (9 Jul)** — bell group → zone routing via **AHM matrix crosspoints (Option 3)**: RE the matrix-send command (tshark + diff), then set crosspoints per bell. The group buttons edit config today but the driver still plays every bell to the whole school.

**Still to do (from yesterday's ideas, not built)** — Setup login (username/password), WiFi in network settings, drag-and-drop file transfer with auto audio conversion, phone-home portal (blocked — no portal details).

#bns #project/bell-commander
