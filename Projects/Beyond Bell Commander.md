# Beyond Bell Commander

Productised school PA + bell scheduling system under the BNS brand.

## Architecture v2 (locked — Jul 2026)
Designed from scratch for maintainability and stability. Three lines:
**Headless Linux appliance (engine) → TCP :51325 → AHM (playback + Dante routing) → per-block amps.** Any browser is the face.

### Head end
- Headless Pi / Linux appliance in the rack beside the AHM. No attached display — a dead screen is cosmetic, never a bells fault. Easy swap via golden image + site config.
- **One Python service**: scheduler, AHM driver, EVAC GPIO, phone-home client, serves the UI. FastAPI + SQLite (WAL). Deliberately boring, few pinned dependencies.
- **Scheduler = compiled tick loop, not a framework.** Daily (and on config change) the engine compiles the day's plan into a flat list ("ring bell X to group Y at 11:00:00"); a 1-second tick executes it. ~100 readable lines, exhaustively unit-tested: every 2026 date, every override, DST. "Did we break bells?" is a CI answer.
- **Fake AHM** test server (speaks the documented protocol) — full schedule→driver→DSP path exercised on every commit, no hardware. Field bugs reproduced + pinned as regression tests.
- Stability layers: Pi hardware watchdog → systemd → app self-check; read-only rootfs; no auto-updates (deliberate tested release images); chrony; fully functional with zero internet (telemetry queues locally).
- Config = one schema-validated JSON. Code / config / data never mix. Reflash + config.json = site restored.

### Displays (fully decoupled — all just browsers on the appliance URL)
- Office PC / iPad — zero cost
- ELC SMT101 10.1" Android wall panel (Fully Kiosk) — the 10" upgrade AND the Android-tablet option in one SKU (see [[ELC Smart Panels]])
- Pi + 7"/10" touch all-in-one where a single-box look is wanted (PiFace kiosk pattern)

### Audio
- **AHM-16** (product floor): internal WAV bell player (~2.5 GB storage), matrix, priority ducking. Driver = documented TCP :51325 (ahm_player.py groundwork).
- **Dante I/O card** → decentralised Dante amps per block → local 100V speakers. One network drop per block; block failure silences only that block. Netgear M4250 (IGMP, PTP QoS).
- **Music bells** (schools change songs each term, self-service): Audinate AVIO USB on the appliance puts it on Dante as a source. Staff upload MP3 in UI → ffmpeg normalise/trim/fade → assign to any bell. Engine health-checks the path and falls back to AHM stored tone automatically. (Music licensing = school's responsibility; APRA AMCOS education licences generally cover NSW schools.)
- **Fallback bells**: AHM **Manage > Events** scheduler (50 events/config, NTP-synced, Track Playback event type) holds a dormant mirror of the core timetable. Activation via labelled SoftKey or remotely. Bells survive appliance failure with no extra hardware. *(Algo 8301 removed from plan — AHM Events does its job for free.)*
- No file upload to AHM over documented TCP — uploads ride AH-Net (System Manager). Product answer: fixed sound-slot bank loaded at commissioning + UI remaps slots; true uploads = remote System Manager support task. Music bells cover the "custom audio" demand anyway.

### Safety
- FIP dry contact (N/C) → appliance GPIO → holds all bells. Supplementary only — certified EWIS (AS 1670.4) always independent and untouched.
- EVAC events logged forever (see [[Bell Commander Telemetry]]).

### Remote management (decided)
- Outbound-HTTPS phone-home → office-hosted Docker portal (fixed IP; confirm contractually static). External uptime watchdog + UPS non-negotiable. VPS migration = DNS change.
- Remote access: 4G out-of-band gateway w/ Tailscale on rack side; school-network Tailscale only with written IT approval.

### Languages
- **Python** everywhere on the backend (appliance service + portal, one monorepo, shared models) — matches existing BNS tooling (Flask, ReportLab, ffmpeg, ahm_player.py).
- **React** UI (Schoolyard), built to static files, vendored per release — no build tooling on the Pi.
- Node-RED = bench/prototyping tool only, never in the shipped image.

## UI
- Direction locked: **Schoolyard** — blazer green + bell brass on warm paper, plain language ("Ring the bell", "held", "skipped"), tactile buttons. Working React demo: [[Bell Commander Demo UI]].

## Bench checklist (prototype)
- Pi 5 · AHM-16 + Dante card · one Dante amp · M4250 · GPIO EVAC via optocoupler
- AHM: set **static IP** (currently 192.168.1.168 on the bench after a DHCP move — pin it; commissioning checklist item: "AHM IP fixed + recorded in site config")

### Office bench access (confirmed 2026-07-07, over Sophos VPN)
Reached from home over the **Sophos SSL VPN** — office subnets `172.16.200.0/24` + `172.16.220.0/24`, gateway `172.16.240.129`. VPN must be up or nothing below is reachable.
- **Prototype Pi:** `ssh pi@172.16.200.217` — key-based (ed25519 in `/home/pi/.ssh/authorized_keys`), no password prompt; passwordless sudo. Pi 5 Model B Rev 1.1, Raspberry Pi OS / Debian 12 bookworm, kernel `6.12.75+rpt-rpi-2712`. Portrait DSI touch display (`.xinitrc`: `xrandr --output DSI-2 --rotate left` + touch coordinate-transform matrix). **Now the deployed Beyond Bell prototype** (see Prototype build below): `bellcommander.service` on :80, Chromium `--kiosk` repointed to the appliance UI (`http://127.0.0.1/`). Old kiosk build removed 8 Jul — `~/kiosk` deleted (was pushed to its own repo), `kiosk-app.service`/`nodered.service`/`adb-connect.service` disabled. No Docker on this Pi.
- **Office AHM:** `172.16.200.127` (confirmed online: TCP :51325 open, no SSH/web). Separate unit from the bench AHM at 192.168.1.168. Driver reuses [[AHM Reverse Engineering]] (`ahm_player.py`/`ahm_server.py`, persistent TCP :51325).
- Verify: WAV storage/backup-restore with sounds loaded · direct playback-channel trigger over TCP · Events fallback end-to-end (Manage > Events, Track Playback) · whether Events are writable over AH-Net (capture System Manager while creating one) · AVIO music path + auto-fallback
- Keep one persistent AHM TCP session with heartbeat/reconnect

## Prototype build — deployed 8 Jul 2026
First working appliance built and running on the office Pi (`172.16.200.217`, systemd `bellcommander.service`, serves on **:80**), driving the **real office AHM** (`172.16.200.127`). Code: `Desktop\Beyond Bell Commander\engine` (git repo since ~12 Jul — work on **beta** branch, bare remote `tooling` on [[Tooling Docker Host]]; `README.md` + `DEPLOY.md` inside). Build the React UI on the dev box → vendor into `engine/bellcommander/static` → redeploy over the VPN. Survives reboot (verified).

### Engine (hardware-free core, ~78 tests, "did we break bells?" is a CI answer)
- `compile_day(config, date)` → sorted bell list or a silent reason. Gating: **no-bell date → special day → weekend → out of term → template**. (A *special day* = per-date template; forces a ringing day, overriding weekend/holidays — fixed 8 Jul after it silently no-showed out of term.)
- `Engine.tick(now)` — 1-second loop, fires each bell once inside a grace window (never rings a stale bell after a restart), EVAC/silence/skip/manual overrides, recompiles at midnight, DST-correct via `zoneinfo`.
- `SiteConfig` = one pydantic-validated JSON (zones, groups, sounds, day templates, calendar). Bad references fail at load.
- **AHMDriver** — persistent Ext-Control TCP :51325 (play SysEx, mute, level, get-level heartbeat, get-name). Fails soft, never crashes the tick loop. **FakeAHM** speaks the same protocol → the whole schedule→driver→AHM path is tested with no hardware. Real office AHM replied to get-level in MIDI *running status* (7-byte, not 9) — driver fixed to accept it.
- Audit log = **SQLite (WAL)**, every ring / skip / hold / EVAC / manual, UTC at source.
- **FastAPI** wraps it (blocking tick runs in an executor thread) and serves the Schoolyard React UI.

### UI (Schoolyard React, served from the appliance)
Tabs: **Today** (live countdown, chalkboard timeline, Ring/Skip/Silence/EVAC), **Calendar** (year view from the real calendar), **Zones** (live mute — switch goes red when muted), **Log**, **Setup** (commissioning), **System**.
- **Setup** sub-tabs — School / Sounds & Zones / Timetable / Calendar — with one Save that validates and **hot-reloads live** (a bad edit is rejected, running config keeps ringing).

### Commissioning = "point at the AHM + the DoE"
All importers fetch *candidates for review*, then save through the validated config write.
- **Bell sounds ← AHM library** (AHNet :51321 track list). Office AHM: Period_Bell, Lock_Down, Lock_In, EVAC, All_Clear, Shelter-in-place.
- **Zones ← AHM zone names** (Ext-Control get-name). Office AHM: Gym, Canteen, Kiss&Ride, B/C/D Block Int/Ext (ch 1-8). Renaming a zone + Save **writes the name back to the AHM** (AHNet name-manager → shows in System Manager). Each zone carries an "AHM ch" so mutes/routing hit the right output.
- **Timetable ← the school's bell-times page** (restricted to `schools.nsw.gov.au`). Parses the DoE CMS layout; times reliable, labels reviewed.
- **Term dates ← NSW DoE calendar** (`education.nsw.gov.au/schooling/calendars/{year}`, Eastern/Western). **Public holidays ← NSW** (Nager.Date, filtered to NSW), now with the holiday *name* stored per date.

### System settings
- **Network**: DHCP/static via NetworkManager with a **90 s auto-revert safety** (a bad static IP can't lock out a remote appliance); MAC shown.
- **Display**: brightness + **sleep timer** (blank after N idle minutes, wake on touch) on the DSI panel; backlight is `pi`-writable, an X-side `xprintidle` watcher does the blanking.
- **Time**: **custom NTP server** (systemd-timesyncd drop-in), with clock-sync status shown — silent drift = broken bells.

### Open items
- **Bell group → zone routing — LIVE (10 Jul).** Crosspoint command RE'd ("Mixer" service, `object = 0x5000 + row*0x40 + col`, payload 01/00, 16×16 — see [[AHM Reverse Engineering]]) and activated: `matrix_source_row=0` = the AHM's **Input 1 "Bells"** row; zones map by AHM channel (col = ch−1). Proven end-to-end on the office AHM — a bell sounds only in its group's zones (verified via XPoint grid + zone meters). Zones re-commissioned to the AHM's real 8 (Gym/Canteen/Kiss&Ride/B·C·D block int/ext). RE write-up: `Desktop\Beyond Bell Commander\AHM\matrix-crosspoint-FINDINGS.md`.
- **App ↔ AHM two-way sync (10 Jul).** App→AHM auto (rename on save, routing on ring); AHM→App auto-adopts System Manager zone renames (delta-safe 30s poll + `POST /api/ahm/sync`). Config stays source of truth.
- **EVAC/Messages + Today page (10 Jul).** Configurable one-touch actions (`config.messages`): Setup → EVAC/Messages tab (per-action sound/group/placement/hold-release-stop/colour, Load-defaults); Today Emergency **popup** (Onsite/Off-Site EVAC, Lock In/Out, Test/Test End, All Clear, STOP) + Rainy Day quick button; removed Skip/Silence; **"Now ringing" indicator**. Runs via `POST /api/message`. *Still to do:* true STOP (AHNet playback-stop 0x1006 — currently only clears/releases); differentiate Onsite vs Off-Site; set the Rainy Day sound.
- **Phone-home portal** — blocked (no BNS portal details yet); audit log is already the source of truth.
- Still the **example site config** (out of term today → silent); needs a real commissioned schedule. Also: systemd watchdog, and confirm the display-sleep self-wake was a real touch vs phantom DSI input.

## Docs produced (all in Attachments)
- [[Bell Commander Product Plan v0.1]] (Algo removed; §6.1 remote mgmt decided — note: pre-dates Architecture v2 in places)
- [[Bell Commander Hardware Options]]
- Signal flow: [[bell-commander-signal-flow-dante.svg]] (current) · [[bell-commander-signal-flow-analogue.svg]] (superseded)
- UI comparison sheet: [[bell-commander-ui-options.html]] → Schoolyard chosen

## Status
- **Working prototype deployed to the office Pi (8 Jul 2026)** — full commissioning UI, driving the real office AHM. See *Prototype build* above. ~78 tests green.
- Demo UI ([[Bell Commander Demo UI]]) superseded by the live Schoolyard React app served from the appliance.
- **9 Jul: AHM matrix crosspoint command reverse-engineered + wired into the driver (gated).**
- **10 Jul: bell→zone routing LIVE on the office AHM; zones commissioned to the real 8; App↔AHM two-way name sync; Today page + Setup EVAC/Messages tab built + deployed.** See *Open items*.
- **10 Jul (pm): SECOND DSP — AtlasIED Atmosphere driver, validated live on a bench AZM4** (172.16.200.161). JSON-RPC :5321; DSP-aware commissioning (Load-from-Atlas Pull + Push build-sheet/Verify, since Atlas names are read-only); group→zone routing via per-message zone bundles (Option A); rang a bell end-to-end; real Stop (`StopMessage`) + per-sound track length. Proves the `Driver` abstraction — a whole DSP was one file + fake + tests, brain unchanged. Also: config Export/Import (Setup→School), log-time UTC→local fix, code under git backed up to bns-tooling. 106 tests.
- Evac audio produced for Campbelltown PS (offsite done, onsite pending voice file)
- **12–15 Jul: live paging + the paging station.** Mic paging over the AHM matrix (each `PagingSource` = input + its own matrix source row; a page is the same crosspoint op as a bell), FastAPI + **/panel** paging UI, deployed rev **0.2.0** to three consoles: office Pi (:80, real AHM), piface Pi (:8095), docker demo on [[Tooling Docker Host]] (:8090, fakeahm). The RK3576S 10.1" tablet (192.168.1.40) is the wall **paging station**, kiosking /panel — see daily notes 2026-07-12..15.
- **16 Jul: THIRD DSP scaffolded — Xilica Solaro QR1** (`XilicaDriver`, TCP :10007 third-party API, raw = dB×1000). Plan: appliance owns the audio (USB→analog in), Xilica only routes. Control-tag xlsx + `Bell System Demo.pjxml` in Tech Resources.
- **17 Jul: MEDIA PLAYER.** The appliance is now its own 4-channel player (Ch1 Bells / Ch2 Preamble / Ch3 BGM / Ch4 EVAC) out a USB audio interface — per-channel volume/device/output, sound library with browser upload, BGM ducks under anything else, preamble chime before pages. **`replace_dsp_player`** = DSPs route only, no internal player needed (the whole point for the Xilica). Amber console warning when media-owned audio can't actually play.
- **18 Jul: mode-aware sound names** (labels always show what will actually play — DSP track name vs media file name, flips live with the mode); **two 2-ch USB interfaces ≡ one 4-ch** (per-channel device); **panel auto-management** (tablet agent heartbeats every 20 s → System tab shows panel + IP, remote reboot, screen wakes 30 min before first bell / sleeps 30 min after last, off on silent days; auto-deployer armed on the office Pi for Monday).
- **18 Jul (pm): Calendar tab owns all scheduling** (Year view + Timetable + Schedules + Terms & Holidays moved out of Setup); **slot-less-sound crash fixed** (uploads with no DSP slot crash-looped the office Pi on restart — drivers now skip them, and a DSP-mode SILENT warning flags scheduled ones: the office rig's 5 re-pointed EVAC actions are silent until the USB interface arrives); **A/B WEEKS** — week rotation up to 4 letters, term's first week = A, each term restarts at A, editor in Calendar → Terms & Holidays, letters in year/month views + header chip. **245 tests.** Daily notes 2026-07-17/18 have the detail.
- **20 Jul: bug hunt + Xilica goes real.** General audit caught two real bugs: Today's "rung" count was a lifetime total (no date filter on the audit query), and Skip Next only tracked bell *time* not which bell (same-time/different-group bells could be mislabeled or ring anyway). System page gained a driver picker (`GET/PUT /api/system/driver`, AHM/Atlas/Xilica) — found & fixed a footgun while building it (saving any System setting silently wiped `device.json`'s driver-selection keys). Then a real Solaro QR1 showed up at 172.16.200.174: committed the (until-then-uncommitted) Xilica driver, cross-checked it against the actual control-tag export + project XML and fixed real bugs (fake per-zone level/mute controls that should've used real ones; `set_zone_mute` was reusing the *same tag as EVAC's safety exclusion*). Nearly took the live school offline swapping the office Pi's driver (8 zones vs the Solaro's 4 — flagged first, did it anyway on request, it crashed exactly as predicted); recovered by turning it into a proper 4-zone proof-of-concept instead (real school's config backed up, restorable — see [[Office Raspberry Pi 5]]). Got a USB audio interface working (fixed a hardcoded-samplerate bug in the process), verified a real bell ring end-to-end including audible file playback, wired every leftover library file to a real Sound/EVAC action, reverse-engineered the Solaro's actual signal-chain wiring (confirmed via a screenshot of the designer's own canvas notes) to nail the exclusion-polarity question for good, and wired live paging into the project's own ducking system — finding a genuine typo in the vendor's own export along the way (zone 1's control is misspelled, every other zone is fine). Final pass caught one more real bug (a stale-cache reset that was missed when the ducking feature was added). **260 tests.** Full day in [[2026-07-20]].
- **21 Jul: EVAC audibility fixed for real, discovery replaces hardcoded IPs.** Per-device media sample rates (two USB DACs disagreeing on native rate no longer break playback); tablet/LED-panel/kiosk-launcher hardcoded IP lists replaced with real LAN-broadcast discovery; EVAC tones now loop until All Clear; preamble follows the page's zones; EVAC redesigned to open per-zone Alert Gain instead of unmuting the paging mic. The big one: root-caused "EVAC ducked but silent" all the way to the vendor's own Ducker documentation (channel 2/Priority is sidechain-only, never mixed to output) and fixed it *in the Solaro project itself* — swapped "Zone N Overall Ducker" for a real Matrix Mixer, confirmed audible live. EVAC Cancel now releases the hold, not just the tone. Full day in [[2026-07-21]].
- **22 Jul: Zones tab gets real level controls, faders re-tapered, meters go real UDP.** External Inputs can now commission Paging Mic/Local; Bells/Alert/Preamble/Spare gained real master Mutes (no more gain-floor-on-mute); EVAC also mutes each zone's ordinary-program bus. Zones tab: per-zone level slider + an expandable per-zone/per-source level matrix (Bells/Alert/Preamble/Paging Mic/Paging Local — Spare deliberately excluded, no per-zone control exists for it), plus an "Inputs" card mirroring the panel's own External Inputs faders. Fader response re-tapered (squared, not linear) across Xilica/AHM/Atmosphere after direct feedback that only the top 30% of travel was usable. RTA meters rebuilt to use the vendor's actual documented SUBSCRIBE/UDP-broadcast mechanism instead of GETRAW polling — two real bugs found in the process (the push payload is decimal dB, not GETRAW's raw×1000; a "Paging Aux" input was silently unmapped). Panel gained dark/light mode (SVG icons, not emoji — the moon glyph didn't render on the tablet's fonts) and its own bug hunt: a dead heartbeat thread (also explained "brightness not working"), and a genuine NIC power-management bug (r8168 failing to wake from D3hot, fixed with a udev rule) unrelated to any Bell Commander code. Separately: a long, ultimately-parked investigation into wiring the paging station's physical Talk switch via GPIO — three real panel crashes later, shelved pending actual board documentation. See [[Tablet Wall Panel]]. 304 tests, 5 commits. Full day in [[2026-07-22]].
- **23 Jul: a very long day — Pink Noise/speech-test tools, faders finished to true unity, DSP-restart fader sync (+ a real protocol bug), panel live-refresh, manual clock set, and an amp keep-alive pulse.** Pink Noise speaker test: found + fixed a "control reads back fine, no audio" bug (its dedicated "Pink Insert Mixer" ships level-floored regardless of mute), reworked to genuine multi-zone, added a sig gen volume slider and a Speech intelligibility test button (reusing the Preamble channel, accepted trade-off: interrupts a live page). Media library: fixed an unsaved-draft bug orphaning uploaded Sounds, found+fixed a real OOM crash (a 45-min file decoded to ~3.8GB RAM, SIGKILLed the appliance 3x — added a duration guard), then redesigned the Sound Library twice (free-text folders → a real tag registry → flattened from grouped tables into one). Fixed a real RTA meter lag (client poll interval, not the backend). Finished the fader-unity fix (100% = 0dB) across every remaining control, and fixed a live-data mojibake bug in two EVAC labels (not a font bug). Found a genuine +80dB hardware fix (USB dongle ALSA output volume was sitting near its floor) and exposed DAC level as a real UI control. Built fader-state sync from the DSP on service restart — which surfaced a real dormant protocol bug (the Solaro replies to every `SET`, never drained, risking desync on the next `GETRAW`). Added a `config_version` counter so the wall panel picks up config changes on its next poll instead of needing a reboot. Added manual clock set next to the NTP card (site has no reliable time-server path). Built an amp keep-alive tone (a downstream amp needs ~1s to wake from sleep, clipping bell starts) — reused the Pink Noise generator's DSP plumbing, then reworked from a continuous tone into a real periodic pulse (interval configurable, default 15 min) on immediate follow-up. 418 tests, committed and pushed as one commit (`f40d7c1`) at day's end. Full day in [[2026-07-23]].
- **26 Jul: Xilica connect-timeout circuit breaker.** `_ensure()` re-dialled the DSP on every single write with no cooldown — against an unreachable unit, starting an EVAC (12-16 writes via `evac_suppress`) took 32s and clearing it 26s, an operator's finger on an emergency control throughout. Fixed with a 5s reconnect cooldown: a failed connect suppresses further attempts until it expires, so a batch pays the connect timeout once rather than once per control. An explicit `connect()`/corrected `set_endpoint()` still clears the cooldown immediately. Not a POC-only bug — any real site whose DSP drops off the network would hit the same stall. Full detail in [[BBC ↔ Xilica Solaro]].
- **30 Jul: physical push-to-talk switch wired up, kiosk discovery made resilient, and a real DSP-address regression found and fixed.** A schematic of the tablet's rear "GND/1/2" terminal finally arrived, unparking the GPIO investigation from 22 Jul — mapped the two lines, found the SoC boots them with an internal pull-down that masks any contact closure, and fixed it with a direct IOC-GRF register write (derived from the RK3576 pinctrl driver source, not guessed) wrapped in a boot-persistent service. Built `ptt-agent.py` to debounce the switch and drive the on-screen Talk button's exact `startTalk()`/`stopTalk()` functions — verified live against the real engine's paging state, not just the local agent. One real incident along the way: a blind register-sweep (before deriving the exact offset from source) hung the tablet hard enough to need a physical power cycle — "read-only" isn't "safe" on this SoC. Full write-up in [[Tablet Wall Panel]]. Separately, a kiosk reboot raced its Bell Commander's own reboot and the launcher gave up to the manual picker after one failed discovery check — `boot()` now retries for ~45s before giving up, still with no hardcoded fallback address. And PTT test traffic exposed a genuinely broken office Pi: `device.json`'s `dsp_host` had regressed to a *different site's* bench-Solaro address (unroutable from the office LAN), and — since that deployment predates the 26 Jul circuit-breaker fix above — every request could hang indefinitely, wedging the whole appliance until a `SIGKILL`. Corrected to the real school Solaro (172.16.200.174), verified online, config backed up. **Then deployed the 26 Jul circuit breaker itself** (diffed first — it was the only drift from the repo, safe as a straight file copy), restarted clean. The office Pi is now current on this driver. Full detail in [[BBC ↔ Xilica Solaro]]. Full day in [[2026-07-30]].

- **31 Jul: per-site branding, a reboot action, a real admin-login bug fixed, and camera-vs-proximity wake-on-approach.** Console + panel can now be white-labelled per school: one accent colour drives the whole `--brass` family (buttons, links, header aurora) plus a hardware-grounded derivation (light/dark/pale tiers matched to the hand-tuned tokens' real measured HSL ratios, not guessed), an optional secondary colour drives the `--warn` family (outside-zone tint, silenced-day badge), and an uploaded logo replaces the default mark — red/alert colours never change, so EVAC/mute always reads the same regardless of a school's palette. Along the way, fixed several places that had Beyond's own blue/amber hardcoded instead of reading those tokens (`.btn.primary`, the panel's Talk button, zone-tile selected state, focus rings, calendar selection, the header aurora's base gradient, the Next-Bell countdown's brass→red fade) — invisible until a real accent override actually tried to use them. New Setup → Branding card (colour pickers + logo upload/reset); console Zones page gained an Inside/Outside badge (the data existed, `/api/config` just never sent it). Verified with a real changeover — Concord High School's actual deep-green/gold palette and crest logo, via the real upload API. Separately: added a "Reboot device" action next to the existing service-restart (`system.reboot_device`, same fire-and-forget shape, its own confirm dialog making clear it's a full host reboot not just a service restart) — and building it surfaced a genuine pre-existing bug, `admin_login` rejected *every* login attempt whenever `BC_ADMIN_PASSWORD` was unset (the documented "no gate" default), so any install that never configured a password had its Unlock button permanently broken despite nothing actually being gated server-side. Fixed, regression-tested. 437 backend tests. Also spent a long stretch trying to wake the tablet panel's screen on approach — the camera turned out to be a genuine dead end (proven live, not guessed) but the onboard proximity sensor now does the job after finding three real bugs in the process. Full write-up in [[Tablet Wall Panel]]. Full day in [[2026-07-31]].

- **1 Aug: wallpaper branding, in-browser sound preview, and a real backups/credentials cleanup.** Branding gained a per-site wallpaper option (`BrandingStore` generalized to hold logo/ and wallpaper/ as siblings under one base dir, both console + panel layer the image behind the existing ambient glows with a `--paper-rgb` readability scrim — deliberately repeating the full `background` shorthand rather than partially overriding it, same shorthand-reset bug class already found once on 31 Jul); deployed to the office Pi, which required migrating its one pre-existing logo file into the new `logo/` subdirectory first so its URL didn't silently break. Sound Library gained a ▶/⏸ in-browser preview per file (`/media-assets` static mount, independent of the existing hardware channel-test path). ptt-agent.py now also wakes a DPMS-blanked panel the instant the physical Talk switch is grabbed. The camera-vs-proximity investigation from 31 Jul got its permanent home: [[Tablet Wall Panel|Tablet/proximity/]]. Separately, a full backups/credentials pass: audited and reorganized the whole Obsidian vault into folders, wrote [[Backups]] documenting how code/site-data/the toolkit server are (and weren't yet) protected, then closed every open item it found — `beta` (30 commits stale, but contributing nothing not already superseded on this branch) merged and fast-forwarded rather than force-pushed; `office-pi-config-backups/` widened to include branding + the sound library, not just config/device JSON; a lost Portainer admin login recovered via its official `helper-reset-password` tool rather than guessed at, a scoped API token minted headlessly via its own API, and a nightly backup cron installed and verified end-to-end; a real GitHub remote (`beyond-bell-commander`, private) finally created after discovering the cached PAT couldn't create *or even push to* a new repo, needing a fresh classic token. Declined to write actual passwords into the vault itself even gitignored — chose a real password manager instead — then rotated the Portainer password via the API as a first real use of that decision. Full day in [[2026-08-01]].

- **1 Aug (pm): Zones/Setup reorganized around zone identity, WAV replaces MP3 for real playback accuracy, and a 10-item hardening pass from two office-Pi audits.** Reworked the confusing overlap between the top-level Zones tab and Setup's "Sounds & Zones" sub-tab one change at a time after a full sidebar-reskin attempt was rejected outright and cleanly reverted — Speaker zones + Bell groups moved into the Zone menu, Sounds folded into Media Player. Added a Today-page quick "upload a track, use it as the daily bell sound" action (permanent, not a today-only override; own window, large music icon, adjustable length, 1s fade in/out via ffmpeg) — fixed a real filename-validation bug it surfaced (`(Sting) Happy.mp3`, an actual library file, rejected by a `SAFE_NAME` regex that contradicted its own error text) and a real "not playing full track" bug, root-caused not to the timer or MP3 decoding in general but to MP3's own VBR/encoder-delay ambiguity — switched the conversion target to uncompressed WAV, verified exact-duration playback both locally and on the office Pi. Sound Library gained a real Length column and the Today page's "now playing" countdown stopped defaulting to a hardcoded 6s guess, both now backed by an actual `soundfile` duration probe. A follow-up office-Pi audit caught a real production 500 on `/api/ring` — an orphaned Sound record pointing at a file the day's own MP3→WAV switch had deleted — which turned into a 10-item hardening pass: cascade-clean on media delete (refuses an in-use file, sweeps up unused orphans), config snapshots before every `apply_config` overwrite, a new `deploy.sh` (tests → tar-backup → deploy → restart → verify, replacing the hand-typed scp pattern that had let ~70 stray `.bak-*` files accumulate on the Pi — ~15 pruned), a `webapp.py` audit turning driver/hardware failures into clean 502s instead of raw 500s across zone-mute/level/pink-noise/speech-test/evac/sound-system/display/restart/reboot, `beta` re-synced, dead CSS removed, and a startup warning when `BC_ADMIN_PASSWORD` is unset on a network-reachable bind. 444/444 tests. A separate batch of pasted "optimization suggestions" for panel.js/xilica.py got checked against the real code rather than taken at face value — most didn't hold up (one described Pink-Noise code that doesn't exist in panel.js at all). Full detail in [[2026-08-01]].

- **2 Aug: External Inputs get a real Panel toggle + explicit Xilica control mapping, a Xilica control reference catalog, and real per-person accounts replacing the shared admin password.** Three pieces, same day. First: External Inputs gained a real "Panel" checkbox (which faders actually show on the wall panel) replacing panel.js's old hardcoded "Paging Aux" name-match, and — after confirming the Ch column only ever meant anything to AHM (Xilica ignores it, fuzzy-matching `name` instead; Atmosphere doesn't implement input control at all, a genuine unguarded 500 turned into a proper 502) — an explicit Control column for Xilica keyed by real source name, not name-matching. Second: a Setup → Xilica Reference tab that imports the vendor's own control-tag export (xlsx, via `openpyxl`) and lists every control the Solaro exposes — cross-referenced against `wired_control_map()` (a full expansion of the driver's own dicts/templates) to flag used vs unused. Verified against the real "Bell System Demo" export: 91 real controls, driver wires 83, and the 8-control gap is exactly `Zone{1-4}PagingMicAlertGain` (the ducker's *depth* setting, vs. the `...Mute` toggle the driver does use) and `Zone{1-4}OutputMeter` — both genuine, not bugs. Export-to-CSV added alongside import. Third, and the big one: the External Inputs Control dropdown's free-text "Custom…" option got replaced with a catalog-backed picker (only real, imported Gain-type controls, never unverified manually-noted ones) after concluding free text was one typo away from silently pointing at a control that doesn't exist — and, since a picked value can now be a real but *unnamed* control tag, `set_input_level`/`_mute_source`/`read_input_level` gained a fallback: any key not in the driver's known six sources is now written to directly as a raw Gain tag (gain-floor mute, no dedicated Mute/Meter — same trade-off `paginglocal` already had), so a future Xilica project's new source becomes drivable the moment its export is imported, no code change needed.
	Then, the same day, a much larger scope-jump landed: real per-person user accounts and a mandatory login gate, replacing the single shared `BC_ADMIN_PASSWORD` as the primary way in. What started as "let non-admins only see Today/Calendar" grew, through several rounds of explicit back-and-forth, into: every page (including Today) now requires a logged-in session; accounts have a username + scrypt-hashed password (stdlib `hashlib.scrypt`, no new dependency) + a role (admin/user); admin reaches everything and gets a new Setup → Users page to add/remove accounts; user only ever reaches Today/Calendar, enforced both by hiding nav *and* by a real redirect guard on direct hash navigation (`#setup` bounces back), not just cosmetic hiding. `BC_ADMIN_PASSWORD` keeps working indefinitely as a fallback admin login, and — carrying forward a regression already found once (31 Jul, the old "no gate" login bug) — a genuinely unconfigured site (no password, no accounts yet) still accepts any credentials as a one-time bootstrap, so a fresh box can never be permanently locked out of its own Setup page. The whole old `X-Admin-Token`/`window.fetch`-monkeypatch mechanism was retired outright rather than left running alongside the new one — one session cookie, one login endpoint, `require_admin` now just a role check on the session a new global FastAPI middleware already attached. The one deliberate, clearly-flagged hole: the physical wall panel is a separate kiosk device, not a login-capable client, and calls many of the same routes the console does — its exact 14 routes (status/config/schedule/inputs/paging/message/stop/evac) stay open with zero session, same as every route already was before this, so it doesn't break a live device. Migrating the existing ~400-test suite turned out cheap in practice: only 5 files construct their own `TestClient`, and a fresh `EngineService`/`UserStore` in tests has zero accounts and no `BC_ADMIN_PASSWORD`, so the same bootstrap-bypass that protects a real fresh install meant each fixture needed exactly one added line (`client.post("/api/login", ...)`) — no test-environment setup at all. 504 tests, committed as one commit (`dcd95ae`) and pushed to both `github` and `tooling`.

- **3 Aug: a hidden kiosk-settings gesture, and the reboot-day bug hunt it triggered.** Added a way back into the wall panel's own launcher/network-settings screen without a power cycle — first as a visible hold-to-open gear button, swapped the same day for an Android-settings-style hidden gesture (11 taps on the logo) after concluding a visible affordance wasn't the point for a corridor device; gated on the tablet's own `netpanel.py` agent being reachable, so it's a silent no-op on any browser that isn't the actual kiosk.
	Deploying and verifying it against the office Pi surfaced a real chain of pre-existing bugs, not new ones. A reboot of the tablet exposed that its last-known server was still the tooling-docker `beta` demo, not the office Pi — traced via the Chromium profile's own LevelDB localStorage over SSH, then fixed live by asking the already-running Chromium instance (single-instance IPC, no kill, no risk to the supervising lightdm session) to open the office Pi's `/panel` directly, and separately by driving the on-screen numpad through `xdotool` (screenshotted via `xwd`+`ffmpeg` over SSH at each step) to reproduce the fix through the real discovery UI rather than just patching storage by hand. That surfaced a genuine `launcher.html` bug: the manual "Enter address" path never appended `/panel` (only the auto-discovery paths did), so a typed IP landed on the full console's login screen instead of the wall panel — and since only that manual path ever called `saveRecent()`, the bad value then poisoned every future discovery-failure fallback too. Fixed by moving `saveRecent()` into `go()` itself, so any successful connection — manual or discovered — remembers its real, resolved URL, plus a `withPanelPath()`/`getLastUrl()` sanitizer so an already-bad saved value self-heals on the next read. Testing the tap gesture itself found a second real bug: the toast fired but the page never actually left — Chromium silently blocks a script-initiated top-level navigation from `http://` straight to `file://`, with no visible error to debug against — fixed by switching to `history.back()` instead, which isn't subject to that restriction since `launcher.html` always reaches `/panel` via a real prior navigation. Last, a separate report that light mode "showed wrong colors entirely" on a Fully Kiosk (Android WebView) tablet traced to the panel never declaring `color-scheme` (the console and `launcher.html` both already did) — without it, WebView applies its own algorithmic "force dark" post-processing to a page it assumes doesn't handle dark mode itself, mangling an intentionally light theme while an already-dark one looks fine by coincidence; confirmed root cause was simply the tablet's OS being set to dark mode. Four commits (`903436e`, `b140dfe`, `edacf17`, `7fff0f7`), 504 tests unchanged throughout — pure frontend/static-asset work plus one `launcher.html` client-side fix, no backend logic touched.

- **3 Aug (pm): disk-fill safeguards for term-by-term bell uploads, appliance health totals, and a proximity-sensor tune-up.** The System page's appliance health card gained a real Disk row (root filesystem via stdlib `os.statvfs`, same shape as CPU/RAM) — which immediately raised the practical question of how to stop the disk actually filling as the client uploads bells term by term. Two layered guards: every media upload now refuses outright if it would leave less than 500MB free system-wide (the real backstop — SQLite's audit log, config saves, and the OS itself all need write room regardless of upload activity), and files loaded via the Today page's own quick-upload action are tracked separately (a `.today_loads.json` sidecar, oldest-first ordering) under their own 500MB budget, evicted oldest-first once full — skipping anything still wired into a live bell or message, since that upload is meant to become the *permanent* daily sound, not a today-only throwaway; runs out of safe candidates and it rejects the upload rather than silently blow the budget. An ordinary Setup → Media Player upload is untouched by that second guard. Same session, the health card grew further on request: total RAM/disk/CPU-core-count now sit next to each percentage bar, not just the bare number. Separately: pulled a live 8-second sample straight off the wall tablet's STK3311 proximity chip over i2c (steady 130-134, matching the 31 Jul findings) to confirm the sensor was healthy, then lowered its wake threshold from 220 to 150 on request for a more sensitive wake — flagged that this leaves a much thinner margin above the observed noise ceiling than the original value was deliberately chosen for, worth watching for spurious wakes.

- **3 Aug (evening): panel heartbeat was silently 401ing since the login gate shipped.** Reported as two symptoms — brightness control not working, panel not showing as connected on the System page — that turned out to be one cause: `/api/panel/heartbeat`, the route `netpanel.py`'s own background daemon on the tablet phones home on every 20s (separate from anything `panel.js` calls), was never added to `_PANEL_OPEN_ROUTES` when the mandatory-login system shipped a few days back — missed originally because the whole exempt list was built by grepping `panel.js` specifically, and this caller is a different device. Every heartbeat since then 401'd silently (the beacon loop swallows request failures with no logging, by design, so nothing surfaced in `journalctl`), which is exactly why both symptoms showed up together: screen policy and brightness both ride the heartbeat response. Added the one missing route, a regression test, redeployed — confirmed live: the panel now reports `online: true`, and its actual backlight (51/255) matches the configured 20% policy exactly.

- **4 Aug: a waveform trim picker for the Today-page bell upload, a branding-color correction, and a Now Playing timing fix caught and reverted the same day.** Quick bell-sound upload gained a real trim step: pick the file, get a client-side waveform (Web Audio decode + canvas draw, no server round-trip), drag two handles or type start/end seconds, preview just that region, then commit — server side gained a `start_s` offset alongside the existing `max_duration_s` so ffmpeg's `-ss`/`-t` trim the exact window instead of always cutting from 0:00. Found and fixed a real bug in it the same session: the selection's grey/blue shading compared each bar's bucket-quantized time against start/end while the handles themselves moved at exact pixel positions, so the mask visibly lagged the marker during a drag — replaced per-bar coloring with a mask drawn at the same pixel coordinates the handles use. The separate "max length" field then became redundant once the picker itself set the window, so it was dropped.
	Bell sound card color pass went through a real correction, not just iteration: recolored to `--red` (the fixed alert token) on request, then corrected once it became clear `--red` isn't part of the per-site branding system at all — Setup's actual customizable "secondary" slot is `--warn*` (`brandingCSS`/`deriveWarnPalette`, the same family driving outside-zone tint and silenced-day badges), so a secondary-colour change was silently doing nothing to the card until it was pointed at the right tokens. Upload button switched to the primary-blue style to match.
	Now Playing progress bar got a real regression, caught fast: tried anchoring elapsed time on the server's `since`/`until` timestamps compared straight against the browser's `Date.now()` instead of measuring elapsed-since-mount — looked like the right fix for "doesn't follow correctly," but broke immediately (bar pinned at 100%, seconds pinned at 0), since nothing guarantees the appliance's clock and the viewing browser's agree. Reverted same day back to the original mount-relative math, which is clock-skew-immune by construction. The original report turned out to just be a stale cached bundle in the browser, not a real bug.
	Separately confirmed the term-dates/public-holidays importers already built into Setup → Calendar work exactly as designed when asked "do we have term dates for 2027" — 2026 is live end-to-end (`education.nsw.gov.au` + Nager.Date), 2027 term dates 404 because NSW hasn't published that year's calendar page yet, but 2027 public holidays already resolve fine (a different, independent data source).
	Panel sidebar restyled from a row of left-aligned icon+text buttons into stacked icon-over-label cards against a supplied reference image (glow on the active tab reusing the panel's existing `--lit-brass` token; EVAC now carries its red styling permanently, not only when selected) — then two follow-on requests the same session: the Zone Exclusions macro button recolored amber (new `.macro.warn`) since it's navigation, not an alert, and both the selected-zone-tile glow and the live-Talk glow toned down with a new `--lit-red-soft` token kept deliberately separate from the shared `--lit-red` other alert elements still use at full strength.
	519 tests, seven commits pushed to both `github` and `tooling` (`213f6ec`..`9fe304e`). Pulled a fresh dated snapshot to `office-pi-config-backups/2026-08-04-office/` per the established convention (config/device/branding/media/service) — still the Xilica POC config, unchanged since 20 Jul.

## Related
- [[Bell Commander Telemetry]] · [[Bell Commander Demo UI]] · [[Bell Commander UI Spec]] · [[AHM Reverse Engineering]] · [[BBC ↔ Xilica Solaro]] · [[Campbelltown PS]] · [[ELC Smart Panels]] · [[Tablet Wall Panel]] · [[Office Raspberry Pi 5]] · [[Tooling Docker Host]] · [[Backups]]

#bns #project/bell-commander
