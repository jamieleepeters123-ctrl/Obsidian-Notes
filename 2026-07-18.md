# 2026-07-18 — Mode-aware sound names, 2×2ch audio, panel auto-management

Saturday session on [[project_bell_commander_engine|Bell Commander]] (no bells today, so live edits were safe). Three things from Jamie's morning notes:

**1. Names now always name what will actually play.** My blanket rename yesterday was the wrong shape — switching back to the DSP's internal player kept showing media file names. Reverted: `Sound.name` is the DSP's track name again, and the *display* name is computed per mode — DSP mode shows `1. Period_Bell` (the AHM's own track), media mode shows `School Bell` (the file). Flipping the toggle flips every label live (menus, next-bell, audit). Verified on the office Pi.

**2. Two 2-channel USB interfaces = one 4-channel.** Jamie asked; answer is yes, and it's built: each media channel can name its own device ("" = main), the backend opens one PortAudio stream per distinct device, and uniqueness is per (device, output). Device column added in Setup → Media Player. No need for two media players — one player, channels spread across devices.

**3. Beyond panels auto-register and are server-managed.** The tablet's netpanel agent gained a `beacon()` — heartbeats the engine every 20s (id from machine-id). Engine keeps a registry: **System → Beyond panels** shows name/IP/online, a **Reboot** button, and the power policy: **screens wake 30 min before the first bell and sleep 30 min after the last** (configurable; silent days sleep all day; touch still wakes — edge-triggered so the policy doesn't fight the user). Brightness pushed from the server too. **Pending:** the tablet was offline (powered down for the weekend) — deploy the updated `Tablet/net/netpanel.py` + `systemctl restart netpanel` when it's back, then it appears in the console on its own.

Noted: office config has All Clear + Shelter in Place pointed at `School Bell.wav` (Jamie's assignment, likely to clear the ⚠ warnings) — fine in DSP mode, but in media mode they'd play the bell sound. Real files wanted. `replace_dsp_player` left OFF for Monday. 236 tests, `bcbb902`, all three consoles deployed.
