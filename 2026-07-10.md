Bell Commander UI Changes to be made
 - Today Page
	 - Remove Skip Next Bell
	 - Remove Silence today
	 - Add Rainy Day
	 - Emergency need to pop up a bubble with
		 - Onsite 
		 - Off-Site
		 - Lock In
		 - Lock Out
		 - Test
		 - Test End
		 - All Clear
		 - STOP
- Create a new EVAC/Message Tab in setup to set bell sound for each EVAC. allow to add remove EVAC's. We need a place to set custom bell for rainy day button. here will do for now.

---

### Built 2026-07-10 (deployed to office Pi)

**Bell → zone routing is LIVE.** RE'd the AHM matrix crosspoint command (see [[AHM Reverse Engineering]]) and wired it in: `matrix_source_row=0` ("Bells" input), zones map by AHM channel. Proven end-to-end — a bell now sounds only in its group's zones. Zones re-commissioned to the AHM's real 8 (Gym/Canteen/Kiss&Ride/B·C·D block). Two-way name sync App↔AHM (auto-adopt System Manager renames, delta-safe, 30s poll + `POST /api/ahm/sync`).

**Today page** — done ✓:
- Removed **Skip Next** + **Silence today** ✓
- Added **🌧 Rainy Day** quick button ✓
- **Emergency** now opens a **popup** with the 8 actions (Onsite/Off-Site EVAC, Lock In/Out, Test/Test End, All Clear, STOP) ✓
- NEW: **"Now ringing" indicator** — green pulsing banner shows the sound → zones while a bell plays (`status.playing`, ~6s window) ✓

**Setup → EVAC/Messages tab** — done ✓: add/remove/edit actions; per-action **sound + zone group + placement (popup vs quick button) + hold/release/stop + colour**; **Load defaults** seeds the standard EVAC set mapped to the AHM sounds. Config-driven — Today reads `config.messages`, runs via `POST /api/message`. Emergencies default to the whole-site (largest) group. 93 tests.

**"Now ringing" redesigned** ✓ — inline bubble next to *Ring the bell*: bell name, a bar that fills over the play window, live countdown, and a **Stop** button. Stop is a **real** AHNet playback-stop (`Playback Manager` cmd `0x1006`, per channel) via `ahnet.stop_playback` → `AHMDriver.stop()` → `POST /api/stop`; also wired into the STOP/Test End message actions. Verified live on the office AHM.

**Still to do:**
- **Onsite vs Off-Site** behave the same (both play EVAC + hold) — differentiate later.
- **Rainy Day** sound not set (no wet-weather track on the AHM yet) — set it in Setup → EVAC/Messages once recorded.
- From note: Emergency bubble polish; consider two-tier auth before this ships (installer vs staff).



